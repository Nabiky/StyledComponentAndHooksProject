import React from 'react';

import FlexRow from '#lib/FlexRow';

const SelectableCardGroup = props => <FlexRow desktopSpacing={2} tabletSpacing={1} {...props} />;

export default SelectableCardGroup;
