import styled from 'styled-components';
import GridColumn from '#lib/GridColumn';

const GridColumnRightAlign = styled(GridColumn)`
    justify-content: flex-end;
    align-items: flex-end;
`;

export default GridColumnRightAlign;
